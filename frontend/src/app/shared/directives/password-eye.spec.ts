import { PasswordEye } from './password-eye';

describe('PasswordEye', () => {
  it('should create an instance', () => {
    const directive = new PasswordEye();
    expect(directive).toBeTruthy();
  });
});
